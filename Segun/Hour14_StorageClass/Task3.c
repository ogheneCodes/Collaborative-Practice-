#include <stdio.h>

int main(void)
{
	x=0, y=0;
	x=0, y=1;
	x=0, y=2;
	x=0, y=3;
	x=0, y=4;
	printf("This is what it prints to the console,
			and the reason it prints this is 
			because the int x loses its storage once
			the block is closed, while the static int x
			retains the original value from the permanent storage\n");
	return o;
}
