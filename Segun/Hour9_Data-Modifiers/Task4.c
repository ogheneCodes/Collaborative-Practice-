#include <stdio.h>

int main(void)
{
	int a;
	int b;

	a = -307;
	b = -653;
	printf("The hex value and signed int equivalent of a: %#X %d\n", a, a);
	printf("The hex value and signed int equivaent of b: %#X %d\n", b, b);
	return 0;
}
