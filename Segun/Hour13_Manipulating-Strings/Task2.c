#include <stdio.h>

int main(void)
{
    char vat[] = {'A', ' ',
                'F', 'A', 'L', 'L', '\0'};
    
    strlen(char);
    printf("The size of Char vat is: %d bytes", vat);
    return 0;
}
