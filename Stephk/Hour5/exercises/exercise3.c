#include <stdio.h>
int main()
{
    int x, y, z;
    x = 15;
    y = 150;
    z = 1500;

    printf("Print in hex format %x\n", x);
    printf("Print in hex format %X\n", y);
    printf("Print in hex format %x\n", z);
     return 0;
}
