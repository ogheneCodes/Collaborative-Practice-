#include <stdio.h>

int main () {
    // int ch = 15;
    // int ch1 = 150;
    // int ch2 = 1500;
    // printf("The hex format for ch is: %x\n", ch);
    // printf("The hex format for ch1 is: %x\n", ch1);
    // printf("The hex format for ch2 is: %x\n", ch2);
    printf("The hex format for 15 is: %x\n", 15);
    printf("The hex format for 150 is: %x\n", 150);
    printf("The hex format for 1500 is: %x\n", 1500);
    return 0;
}