#include <stdio.h>

/**
* main - prints the number both in scientific and floating point.
* Return 0.
*/

int main(void)
{
    double dbl_num = 123.456;

    printf(" The value if floating point is: %f\n", dbl_num);
    printf(" The value in scintific notation is: %e\n", dbl_num);
}