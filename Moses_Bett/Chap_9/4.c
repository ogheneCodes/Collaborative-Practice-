#include <stdio.h>
int main()
{
	int x;
	int y;
	int z;
	x = 95;
	y = -73;

	printf("display negative integers in hex format: %#x\n", z);
	printf("display negative integers in hex format: %d\n",z);
	printf("display negative integers: %u\n", z);
	 
}
