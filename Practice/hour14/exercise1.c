/*
Given the following:
• An int variable with block scope and temporary storage
• A constant character variable with block scope
• A float local variable with permanent storage
• A register int variable
• A char pointer initialized with a null character
write declarations for all of them
*/

#include <stdio.h>

int main()

{
	int tobzid = 0; //An int variable with block scope and temporary storage
	const char joseph = '85'; // A constant character variable with block scope 
	static float sheila = 22.5; //A float local variable with permanent storage
	register int moses = 35; //A register int variable
	char* isaac = 0;
	return (0);
}
