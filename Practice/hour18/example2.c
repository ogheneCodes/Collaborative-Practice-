#include <stdio.h>
#include <stdint.h>

int main(){
    enum name{
        max = 10,
        moses = 20,
        john = 32, 
        segun,
        juma,
    };
    printf("%d,%d,%d,%d,%d\n\n", max, moses, juma, segun, john);

}

int main(int argc, char *argv[]){
    // code
    // argc argv
}