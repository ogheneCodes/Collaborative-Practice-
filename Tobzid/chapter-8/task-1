#include <stdio.h>

/**
* finding the value of ~x.
* and finding the value of ~y.
* return 0.
*/

 main()
 {
     int x = 0xEFFF;
     int y = 0x1000;

     printf("the value after evaluating x is%d\n", ~x);
     printf("the value after evaluating y is %d\n", ~y);
     return 0;
 }
