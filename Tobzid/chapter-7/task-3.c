#include <stdio.h>

main ()
{
	int c;
	c = ' ',
	printf("Enter a character:\n (Enter k to exit)\n");
	while (c != 'x'){
	c = getc(stdin0;
	putchar(c);
}
printf("\nout of the while loop, Bye!\n");
return 0;
}
