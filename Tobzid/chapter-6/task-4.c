#include <stdio.h>

main()
{
	int x;
	x = 1; x++;
	printf("x = x++ produces: %d\n" x = x++);
	printf("now x contains: %d\n" x);
	printf("x = x++ produces: %d\n" x = x++")
	printf("now x contains: %d\n", x);
}
