#include <stdio.h>

/**
 * main - code
 * Description: Program to print out the corresponding
 * two characters of 72 and 104
 * 
 * Return: 0
 */
int main(void)
{
    int a = 72;
    int b = 104;

    printf("The ASCII character for 72 is %c\n", a);
    printf("The ASCII character for 104 is %c\n", b);

    return (0);
}