#include <stdio.h>

/**
4. Rewrite the program in Listing 12.5. This time put a string of characters, I like
C!, on the screen.

*/

int main(void)
{
    char arr_ch[15] = "I like C!";
    puts(arr_ch);

    return 0;
}
