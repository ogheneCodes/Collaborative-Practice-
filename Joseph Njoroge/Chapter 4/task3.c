You cannot since this goes above the value that the compiler is able to compile, hence garbage will be produced#include <stdio.h>
/**
* main - prints char given the numeric values.
* Returns 0.
*/

int main(void)
{
    printf("The char for the 72 value is %c\n", 72);
    printf("The char for the 104 value is %c\n", 104);

}.