#include <stdio.h>

/**
* main - prints the numbers in hex format.
* return 0.
*/

int main(void)
{
    printf("The value 15 in hex format is: %#x\n", 15);
    printf("The value 150 in hex format is: %#x\n", 150);
    printf("The value 1500 in hex format is: %#x\n", 1500);
    return 0;
}